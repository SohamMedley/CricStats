README.md

Markdown

\# CricStats Execution Pipeline



1\. Install dependencies:

&#x20;  ```bash

&#x20;  pip install -r backend/requirements.txt

Place your service account configuration key JSON at the root directory and name it firebase\_creds.json.



Boot up the server:



Bash

python -m backend.app

Access via mobile emulator browser at http://localhost:5000.

